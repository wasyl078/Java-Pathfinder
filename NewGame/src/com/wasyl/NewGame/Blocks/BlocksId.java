package com.wasyl.NewGame.Blocks;

//enum wszystkich dostępnych rodzajów bloków
public enum BlocksId {
    BackgroundBlock, WallBlock, PlayerBlock, EnemyBlock, PathBlock, FireBlock, OldBlock,FancyMazeMakingBlock, BombBlock
}
